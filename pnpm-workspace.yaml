# Approve build scripts for toolchain deps pnpm blocks by default.
allowBuilds:
  esbuild: true
  sharp: true

# Dedupe to one vite: Astro 5.18 pins vite@6, but Vitest pulls vite@7 whose
# `test` type augmentation then lands on the wrong vite and breaks `astro check`.
overrides:
  vite: ^6.4.1
